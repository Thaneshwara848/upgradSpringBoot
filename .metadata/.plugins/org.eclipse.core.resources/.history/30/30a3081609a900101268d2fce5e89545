package com.example.demo.repo;

public interface MyRepo {

	// findAll ------------> Select * from EMployee ;
	// findById------------> select * from employee where id = ? 
	// save ---------------> insert into employee values(111,'Thanesh',25,50000,'Dev');
	// deleteAll ---------->  delete from employee;
	// deleteById----------> delete from employee where id = ? 
	// upsert--------------> insert / upate // update employee set salaery = ? where id = ? 
	// existByid 
	// etc....! so many ready made methids are avalible ? 
}
